{
    "navigationFallback": {
        "rewrite": "/index.html"
    },
    "routes": [
        {
            "route": "/{shortUrl}",
            "redirect": "/apiIsolated/UrlRedirect/{shortUrl}"
        }
    ]
}

